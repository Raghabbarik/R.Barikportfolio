
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to the portfolio content
    match /portfolioContent/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
